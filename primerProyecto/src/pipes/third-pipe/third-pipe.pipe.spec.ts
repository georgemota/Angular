import { ThirdPipePipe } from './third-pipe.pipe';

describe('ThirdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ThirdPipePipe();
    expect(pipe).toBeTruthy();
  });
});
