import { FirtsPipePipe } from './firts-pipe.pipe';

describe('FirtsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirtsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
